// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: vibesync/sync/v1/sync.proto

package syncv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
	v1 "vibesync/gen/go/vibesync/sync/v1"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SyncServiceName is the fully-qualified name of the SyncService service.
	SyncServiceName = "vibesync.sync.v1.SyncService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SyncServiceSubscribeProcedure is the fully-qualified name of the SyncService's Subscribe RPC.
	SyncServiceSubscribeProcedure = "/vibesync.sync.v1.SyncService/Subscribe"
	// SyncServiceHeartbeatProcedure is the fully-qualified name of the SyncService's Heartbeat RPC.
	SyncServiceHeartbeatProcedure = "/vibesync.sync.v1.SyncService/Heartbeat"
	// SyncServiceCommandProcedure is the fully-qualified name of the SyncService's Command RPC.
	SyncServiceCommandProcedure = "/vibesync.sync.v1.SyncService/Command"
	// SyncServiceRecoverProcedure is the fully-qualified name of the SyncService's Recover RPC.
	SyncServiceRecoverProcedure = "/vibesync.sync.v1.SyncService/Recover"
)

// SyncServiceClient is a client for the vibesync.sync.v1.SyncService service.
type SyncServiceClient interface {
	// Subscribe to authoritative updates for a room. Server-streaming: the
	// client holds this open for the lifetime of its session and receives
	// SubscribeResponse messages (incremental updates + periodic snapshots).
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error)
	// Heartbeat. Clients send this on a fixed cadence (default 1s, configurable
	// per room). The server uses the round-trip timing to estimate RTT and
	// clock offset, and the embedded client_media_time to compute drift.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
	// Request an authoritative state change. Originator must hold the host role
	// (or a moderator). The server validates, applies, bumps the epoch, and the
	// resulting SyncFrame is broadcast via the Subscribe stream.
	Command(context.Context, *connect.Request[v1.CommandRequest]) (*connect.Response[v1.CommandResponse], error)
	// Recover after a disconnect. The client supplies the last epoch it applied
	// successfully; the server replays any frames since (bounded) or, if the
	// gap is too large, returns a full snapshot.
	Recover(context.Context, *connect.Request[v1.RecoverRequest]) (*connect.Response[v1.RecoverResponse], error)
}

// NewSyncServiceClient constructs a client for the vibesync.sync.v1.SyncService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSyncServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SyncServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	syncServiceMethods := v1.File_vibesync_sync_v1_sync_proto.Services().ByName("SyncService").Methods()
	return &syncServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.SubscribeResponse](
			httpClient,
			baseURL+SyncServiceSubscribeProcedure,
			connect.WithSchema(syncServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
		heartbeat: connect.NewClient[v1.HeartbeatRequest, v1.HeartbeatResponse](
			httpClient,
			baseURL+SyncServiceHeartbeatProcedure,
			connect.WithSchema(syncServiceMethods.ByName("Heartbeat")),
			connect.WithClientOptions(opts...),
		),
		command: connect.NewClient[v1.CommandRequest, v1.CommandResponse](
			httpClient,
			baseURL+SyncServiceCommandProcedure,
			connect.WithSchema(syncServiceMethods.ByName("Command")),
			connect.WithClientOptions(opts...),
		),
		recover: connect.NewClient[v1.RecoverRequest, v1.RecoverResponse](
			httpClient,
			baseURL+SyncServiceRecoverProcedure,
			connect.WithSchema(syncServiceMethods.ByName("Recover")),
			connect.WithClientOptions(opts...),
		),
	}
}

// syncServiceClient implements SyncServiceClient.
type syncServiceClient struct {
	subscribe *connect.Client[v1.SubscribeRequest, v1.SubscribeResponse]
	heartbeat *connect.Client[v1.HeartbeatRequest, v1.HeartbeatResponse]
	command   *connect.Client[v1.CommandRequest, v1.CommandResponse]
	recover   *connect.Client[v1.RecoverRequest, v1.RecoverResponse]
}

// Subscribe calls vibesync.sync.v1.SyncService.Subscribe.
func (c *syncServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// Heartbeat calls vibesync.sync.v1.SyncService.Heartbeat.
func (c *syncServiceClient) Heartbeat(ctx context.Context, req *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return c.heartbeat.CallUnary(ctx, req)
}

// Command calls vibesync.sync.v1.SyncService.Command.
func (c *syncServiceClient) Command(ctx context.Context, req *connect.Request[v1.CommandRequest]) (*connect.Response[v1.CommandResponse], error) {
	return c.command.CallUnary(ctx, req)
}

// Recover calls vibesync.sync.v1.SyncService.Recover.
func (c *syncServiceClient) Recover(ctx context.Context, req *connect.Request[v1.RecoverRequest]) (*connect.Response[v1.RecoverResponse], error) {
	return c.recover.CallUnary(ctx, req)
}

// SyncServiceHandler is an implementation of the vibesync.sync.v1.SyncService service.
type SyncServiceHandler interface {
	// Subscribe to authoritative updates for a room. Server-streaming: the
	// client holds this open for the lifetime of its session and receives
	// SubscribeResponse messages (incremental updates + periodic snapshots).
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error
	// Heartbeat. Clients send this on a fixed cadence (default 1s, configurable
	// per room). The server uses the round-trip timing to estimate RTT and
	// clock offset, and the embedded client_media_time to compute drift.
	Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error)
	// Request an authoritative state change. Originator must hold the host role
	// (or a moderator). The server validates, applies, bumps the epoch, and the
	// resulting SyncFrame is broadcast via the Subscribe stream.
	Command(context.Context, *connect.Request[v1.CommandRequest]) (*connect.Response[v1.CommandResponse], error)
	// Recover after a disconnect. The client supplies the last epoch it applied
	// successfully; the server replays any frames since (bounded) or, if the
	// gap is too large, returns a full snapshot.
	Recover(context.Context, *connect.Request[v1.RecoverRequest]) (*connect.Response[v1.RecoverResponse], error)
}

// NewSyncServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSyncServiceHandler(svc SyncServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	syncServiceMethods := v1.File_vibesync_sync_v1_sync_proto.Services().ByName("SyncService").Methods()
	syncServiceSubscribeHandler := connect.NewServerStreamHandler(
		SyncServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(syncServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceHeartbeatHandler := connect.NewUnaryHandler(
		SyncServiceHeartbeatProcedure,
		svc.Heartbeat,
		connect.WithSchema(syncServiceMethods.ByName("Heartbeat")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceCommandHandler := connect.NewUnaryHandler(
		SyncServiceCommandProcedure,
		svc.Command,
		connect.WithSchema(syncServiceMethods.ByName("Command")),
		connect.WithHandlerOptions(opts...),
	)
	syncServiceRecoverHandler := connect.NewUnaryHandler(
		SyncServiceRecoverProcedure,
		svc.Recover,
		connect.WithSchema(syncServiceMethods.ByName("Recover")),
		connect.WithHandlerOptions(opts...),
	)
	return "/vibesync.sync.v1.SyncService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SyncServiceSubscribeProcedure:
			syncServiceSubscribeHandler.ServeHTTP(w, r)
		case SyncServiceHeartbeatProcedure:
			syncServiceHeartbeatHandler.ServeHTTP(w, r)
		case SyncServiceCommandProcedure:
			syncServiceCommandHandler.ServeHTTP(w, r)
		case SyncServiceRecoverProcedure:
			syncServiceRecoverHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSyncServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSyncServiceHandler struct{}

func (UnimplementedSyncServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("vibesync.sync.v1.SyncService.Subscribe is not implemented"))
}

func (UnimplementedSyncServiceHandler) Heartbeat(context.Context, *connect.Request[v1.HeartbeatRequest]) (*connect.Response[v1.HeartbeatResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vibesync.sync.v1.SyncService.Heartbeat is not implemented"))
}

func (UnimplementedSyncServiceHandler) Command(context.Context, *connect.Request[v1.CommandRequest]) (*connect.Response[v1.CommandResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vibesync.sync.v1.SyncService.Command is not implemented"))
}

func (UnimplementedSyncServiceHandler) Recover(context.Context, *connect.Request[v1.RecoverRequest]) (*connect.Response[v1.RecoverResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("vibesync.sync.v1.SyncService.Recover is not implemented"))
}
