// @generated by protoc-gen-connect-es v1.7.0 with parameter "target=ts,import_extension=none"
// @generated from file vibesync/sync/v1/sync.proto (package vibesync.sync.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { CommandRequest, CommandResponse, HeartbeatRequest, HeartbeatResponse, RecoverRequest, RecoverResponse, SubscribeRequest, SubscribeResponse } from "./sync_pb";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * @generated from service vibesync.sync.v1.SyncService
 */
export const SyncService = {
  typeName: "vibesync.sync.v1.SyncService",
  methods: {
    /**
     * Subscribe to authoritative updates for a room. Server-streaming: the
     * client holds this open for the lifetime of its session and receives
     * SubscribeResponse messages (incremental updates + periodic snapshots).
     *
     * @generated from rpc vibesync.sync.v1.SyncService.Subscribe
     */
    subscribe: {
      name: "Subscribe",
      I: SubscribeRequest,
      O: SubscribeResponse,
      kind: MethodKind.ServerStreaming,
    },
    /**
     * Heartbeat. Clients send this on a fixed cadence (default 1s, configurable
     * per room). The server uses the round-trip timing to estimate RTT and
     * clock offset, and the embedded client_media_time to compute drift.
     *
     * @generated from rpc vibesync.sync.v1.SyncService.Heartbeat
     */
    heartbeat: {
      name: "Heartbeat",
      I: HeartbeatRequest,
      O: HeartbeatResponse,
      kind: MethodKind.Unary,
    },
    /**
     * Request an authoritative state change. Originator must hold the host role
     * (or a moderator). The server validates, applies, bumps the epoch, and the
     * resulting SyncFrame is broadcast via the Subscribe stream.
     *
     * @generated from rpc vibesync.sync.v1.SyncService.Command
     */
    command: {
      name: "Command",
      I: CommandRequest,
      O: CommandResponse,
      kind: MethodKind.Unary,
    },
    /**
     * Recover after a disconnect. The client supplies the last epoch it applied
     * successfully; the server replays any frames since (bounded) or, if the
     * gap is too large, returns a full snapshot.
     *
     * @generated from rpc vibesync.sync.v1.SyncService.Recover
     */
    recover: {
      name: "Recover",
      I: RecoverRequest,
      O: RecoverResponse,
      kind: MethodKind.Unary,
    },
  }
} as const;

